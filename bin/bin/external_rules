#! /bin/sh

wid=$1
class=$2
instance=$3


# if [ $(bspc query -N -d|wc -l) == "1" ]; then echo "split_dir=south"; fi
bspwm_grid.sh


if [ "$instance" = fontforge ] ; then
	title=$(xtitle "$wid")
	case "$title" in
		Layers|Tools|Warning)
			echo "focus = off"
			;;
	esac
fi
